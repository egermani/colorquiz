<%- headers = ['Created', 'CIEDifference', 'L-delta'] -%>
<%= CSV.generate_line headers %>
<%- Guess.all.order(created_at: :asc).each do |guess| -%>
<%= CSV.generate_line([guess.created_at, guess.delta, Colorable::deltas(guess, guess.spot)[:l]]) -%>
<%- end -%>