$("#results").html("<%= j(render(:partial => 'rounds/guess', :collection => @spot.guesses.color.where(guesser_id: current_or_guest_user).order(:created_at => :desc))) %>");
$("#results").append("<hr/>")
$("#results").append("<%= j(render(:partial => 'rounds/guess', :collection => @spot.guesses.value.where(guesser_id: current_or_guest_user).order(:created_at => :desc))) %>");
